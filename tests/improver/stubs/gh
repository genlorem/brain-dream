#!/usr/bin/env bash
set -uo pipefail

if [[ "${1:-}" == "api" ]]; then
  printf '%s\n' '0123456789abcdef0123456789abcdef01234567'
  exit 0
fi

command cat "${GH_STUB_RESPONSE:?GH_STUB_RESPONSE is required}"
