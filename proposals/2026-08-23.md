# Proposals from dream-introspector

**Date:** 2026-08-23
**Cost (subscription, reference):** $1.2939348
**Tokens:** 14 in / 17394 out
**Dreams analyzed:** 6
**Generated:** 2026-08-23T04:03:49Z

Это предложения ИИ-агента для улучшения brain-dream. **НЕ применяются автоматически.**
Просмотри, оцени, мёржи руками что годится: `git apply`, ручная правка или просто инсайты.

---

Готово, все нужные привязки к строкам подтверждены. Формулирую предложения.

## Proposal #1: Дедуп dream-кандидатов против существующего корпуса Brain (не только против других ночей)

**Что заметил:** `dream-2026-08-19` (топ-9) и `dream-2026-08-22` (заголовок ВСЕЙ ночи) — оба про один и тот же факт: `lesson:vipzal-persona-import-dragonpass-only-and-premium-2026-08-10`. Нода в Brain существует с 10.08, но за 3 ночи он дважды всплыл как «новый» топ-инсайт. Это живое подтверждение уже открытой (`status: active`) `lesson:brain-dream-candidates-not-deduped-vs-corpus` (2026-07-19, обновлено 2026-08-02): дедуп в `dedup_against_registry()` (`orchestrator/brain-dream.sh:1425`) сверяет кандидата только с `.insight-hashes.jsonl` (другие ночи, окно 14 дней, точный content-hash) и с TG-лентой за 5 дней (`decision:brain-dream-digest-feed-dedup`), но никогда — с уже существующими `lesson:`/`decision:`/`procedure:`-нодами. LLM каждую ночь перефразирует факт заново → hash не совпадает → регистр считает его «новым».

**Предложение:** в `dedup_against_registry()` перед проверкой `registry_has_hash` (строка 1436) добавить шаг `brain_search`-эхо-проверки по title/insight кандидата; при явном совпадении с существующей нодой — не пускать в `CANDIDATES_FILE`/registry (или помечать `corpus_echo:true` и исключать из синтеза), как и советует сама lesson-нода.

**Обоснование:** без этого топ-10 продолжит палить слоты синтеза на пересказ старых нерешённых lesson-нод. Побочный эффект: такие «фальшиво-новые» кандиданты имеют уникальный content_hash → `saturation_reached()` (строка 1486) никогда не видит падение прироста → early-stop почти не срабатывает → все 6 ночей недели `sonnet_session_share_pct` держится ровно 30.00–30.50%, независимо от объёма реально нового материала (22.08 candidate_count упал до 107 против обычных 152–154, а бюджет всё равно выбран полностью). Фикс дедупа — это заодно фикс лишнего расхода сессии.

**Риск:** низкий. **Обратимость:** легко (флаг `DREAM_CORPUS_DEDUP`, откат одной строкой).

## Proposal #2: Явный алерт на затяжной Gemini-fallback, а не только строка в логе

**Что заметил:** во всех 6 снах недели (17–22.08) `gemini_passes: 0` — праймари-движок Gemini не сделал ни одной итерации ни разу за неделю, весь синтез шёл через Sonnet-подписку. Существующий алерт из `lesson:brain-dream-silent-death-gemini-cap` (коммит 7fae071) срабатывает только при `RUNS==0 && FAILS>0` (обрыв после старта попыток). Текущий путь — `gemini_available()` (`orchestrator/brain-dream.sh:2183`) как pre-flight ДО входа в цикл; при провале `RUN_ENGINE=sonnet` выставляется раньше первой попытки → `RUNS=0 И FAILS=0` → старый alert-триггер не срабатывает. Есть только `log "stage=generation event=gemini_unavailable fallback=sonnet"` (строка 2327) — в файл, без эскалации. Это ровно тот архетип, который сама система назвала топ-1 инсайтом сна 08-21 («молчаливая деградация вместо явной ошибки») — но не применила к себе.

**Предложение:** завести файл-стрик (`$DREAM_OUT_DIR/.gemini-fallback-streak`) рядом с `.last-run.json`; в ветке `GEMINI_UNAVAILABLE=1` (строки 2323–2328) инкрементировать счётчик; при streak≥3 — TG-алерт («Gemini недоступен N ночей подряд, синтез идёт на Sonnet-подписке ~30%/ночь»). Сброс — при первом успешном `gemini_available()`.

**Обоснование:** стрик уже минимум 6 ночей и никто об этом не уведомлён — узнать можно только вручную заглянув в frontmatter. Каждая такая ночь — ~30% дневного лимита подписки Claude Code потрачено на компенсацию проблемы, которую никто не чинит, потому что не видит.

**Риск:** низкий (чисто аддитивно). **Обратимость:** легко.

## Proposal #3: Отдельный (более частый) интервал проверки насыщения для Sonnet-фазы

**Что заметил:** `sonnet_session_share_pct` не опускается ниже 30.00% ни в одну из 6 ночей — Sonnet-фаза никогда не останавливается по `saturation_reached()`, всегда упирается в жёсткий `sonnet_quota_reached()` (cap 30%=60 вызовов). При дефолте `DREAM_SATURATION_CHECK_INTERVAL=50` (`orchestrator/brain-dream.sh:119`) и `DREAM_SONNET_FALLBACK_RUNS=60` первая и единственная проверка насыщения происходит на 50-м проходе — слишком поздно и слишком редко, чтобы поймать сухую ночь вроде 22.08 (candidate_count упал до 107). Это тот же баг, что зафиксирован в `finding:a2b6af75a12ff930` ещё 2026-06-21 — дефолты с тех пор не менялись, баг жив.

**Предложение:** завести отдельную переменную `DREAM_SONNET_SATURATION_CHECK_INTERVAL` (дефолт 15) и использовать её в sonnet-цикле (вызов `saturation_reached` у строки 2408) вместо общей `DREAM_SATURATION_CHECK_INTERVAL` — даёт 3-4 проверки за 60 проходов вместо одной.

**Обоснование:** дешёвый, независимый стопгэп, который частично защищает бюджет даже до реализации Proposal #1.

**Риск:** низкий. **Обратимость:** легко (одна цифра в конфиге).
